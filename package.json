{
  "name": "livox",
  "version": "0.1.0",
  "private": true,
  "description": "Transforme seus documentos e e-books em audiobooks com vozes naturais e de alta qualidade usando tecnologia de ponta.",
  "main": "main.js",
  "scripts": {
    "dev": "next dev",
    "build:frontend": "cd frontend && npm install && npm run build",
    "copy:build": "powershell -Command \"if (Test-Path 'build') { Remove-Item -Recurse -Force 'build' }; Copy-Item -Recurse 'frontend/out' 'build'\"",
    "start": "next start",
    "lint": "next lint",
    "electron": "electron .",
    "electron:build": "npm run build:frontend && npm run copy:build && electron-builder",
    "electron:build:win": "npm run build:frontend && npm run copy:build && electron-builder --win",
    "electron:build:linux": "npm run build:frontend && npm run copy:build && electron-builder --linux",
    "electron:build:mac": "npm run build:frontend && npm run copy:build && electron-builder --mac",
    "electron:pack": "npm run build:frontend && npm run copy:build && npx electron-packager . LiVox --platform=win32 --arch=x64 --out=dist-packager --overwrite --icon=icon.ico"
  },
  "author": "LiVox Team",
  "license": "MIT",
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "autoprefixer": "^10.0.1",
    "electron": "^37.3.1",
    "electron-builder": "^26.0.12",
    "electron-packager": "^17.1.2",
    "electron-squirrel-startup": "^1.0.0",
    "eslint": "^8",
    "eslint-config-next": "14.0.3",
    "postcss": "^8",
    "tailwindcss": "^3.3.0",
    "typescript": "^5"
  }
}
